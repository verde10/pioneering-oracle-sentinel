[project]
name = "pioneering-oracle-sentinel"
authors = ["OracleSentinel Team"]
description = "A decentralized oracle verification and tracking system"
telemetry = false
requirements = []
[contracts.oracle-sentinel-registry]
path = "contracts/oracle-sentinel-registry.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
